export enum DatabaseType {
  POSTGRES = 'postgres',
  MONGODB = 'mongodb',
}

export enum ActiveStatus {
  PENDING = 'PENDING',
  ACTIVE = 'ACTIVE',
  INACTIVE = 'INACTIVE',
  DELETED = 'DELETED',
}

export enum NotificationStatus {
  YES = 'YES',
  NO = 'NO',
}
